spring:
  datasource:
    url: jdbc:postgresql://db:5432/tech_spec_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: TechSpecJavaSpringFinalV2Application
  logging:
    level:
      root: INFO
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

